import { MedicationValidators } from './medication.validators';

describe('MedicationValidators', () => {
  it('should create an instance', () => {
    expect(new MedicationValidators()).toBeTruthy();
  });
});
